/** SessionStorage key for admin error-logs credentials (client-only). */
export const ADMIN_CREDENTIALS_STORAGE_KEY = "admin-error-logs-credentials";

/** Default page size for admin error-logs table. */
export const ADMIN_ERROR_LOGS_PAGE_SIZE = 50;
